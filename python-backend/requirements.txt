# Python 3.13 compatible FastAPI setup - NO COMPILATION REQUIRED
# Updated versions to fix ForwardRef._evaluate() compatibility issues

# Core FastAPI (Python 3.13 compatible)
fastapi==0.108.0

# ASGI server (pure Python implementation)
uvicorn==0.25.0

# Database (pure Python)
motor==3.3.2
pymongo==4.6.0

# Configuration
python-dotenv==1.0.0

# Data validation (Python 3.13 compatible)
pydantic==2.5.0

# Authentication (minimal, no cryptography)
PyJWT==2.8.0

# File handling (pure Python)
python-multipart==0.0.6

# Minimal data processing (avoid pandas compilation)
# pandas removed - will handle CSV with built-in csv module
openpyxl==3.1.2
xlrd==2.0.1

# Google APIs (minimal set)
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1
google-api-python-client==2.108.0

# Core dependencies (pure Python only)
typing-extensions==4.8.0
click==8.1.7
python-dateutil==2.8.2
six==1.16.0
urllib3==2.0.7
certifi==2023.7.22
requests==2.31.0

# Remove these to avoid compilation:
# - aiohttp (C compilation issues)
# - httptools (C compilation issues)
# - pandas (C compilation issues)
# - anyio (may have C dependencies)
# - h11 (may have C dependencies)
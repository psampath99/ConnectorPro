# ULTRA-STABLE FastAPI setup for Render deployment
# Python 3.11 compatible versions - NO compilation dependencies

# Core FastAPI (proven stable version)
fastapi==0.88.0

# ASGI server (pure Python, no C extensions)
uvicorn==0.20.0

# Database (pure Python)
motor==3.0.0
pymongo==4.2.0

# Configuration
python-dotenv==0.21.0

# Data validation (Pydantic v1 - stable with Python 3.11)
pydantic==1.10.2

# Authentication (minimal, no cryptography)
PyJWT==2.4.0

# File handling (pure Python)
python-multipart==0.0.5

# Minimal data processing (avoid pandas compilation)
openpyxl==3.0.10
xlrd==2.0.1

# Google APIs (minimal set)
google-auth==2.15.0
google-auth-oauthlib==0.7.1
google-auth-httplib2==0.1.0
google-api-python-client==2.70.0

# Core dependencies (Python 3.13 compatible)
typing-extensions==4.8.0
starlette==0.22.0
click==8.1.3
python-dateutil==2.8.2
six==1.16.0
urllib3==1.26.12
certifi==2022.9.24
requests==2.28.1


# ABSOLUTELY NO:
# - pydantic v2 (has Rust dependencies)
# - newer uvicorn (may have C dependencies)
# - aiohttp (C compilation)
# - httptools (C compilation)
# - pandas (C compilation)
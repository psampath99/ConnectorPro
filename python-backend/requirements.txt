# Python 3.13 Compatible FastAPI setup for Render deployment
# Updated to work with Render's Python 3.13 environment

# Core FastAPI (Python 3.13 compatible)
fastapi==0.104.1

# ASGI server (Python 3.13 compatible)
uvicorn==0.24.0

# Database (pure Python)
motor==3.3.2
pymongo==4.6.0

# Configuration
python-dotenv==1.0.0

# Data validation (Pydantic v1 - latest compatible with Python 3.13)
pydantic==1.10.13

# Authentication (minimal, no cryptography)
PyJWT==2.8.0

# File handling (pure Python)
python-multipart==0.0.6

# Minimal data processing (avoid pandas compilation)
openpyxl==3.1.2
xlrd==2.0.1

# Google APIs (updated set)
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1
google-api-python-client==2.108.0

# Core dependencies (Python 3.13 compatible)
typing-extensions==4.8.0
starlette==0.27.0
click==8.1.7
python-dateutil==2.8.2
six==1.16.0
urllib3==2.0.7
certifi==2023.11.17
requests==2.31.0

# ABSOLUTELY NO:
# - pydantic v2 (has Rust dependencies)
# - newer uvicorn (may have C dependencies)
# - aiohttp (C compilation)
# - httptools (C compilation)
# - pandas (C compilation)